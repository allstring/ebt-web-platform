# 모든 텍스트 파일은 자동으로 줄바꿈 처리
* text=auto

# 쉘 스크립트는 항상 LF
*.sh text eol=lf

# 자바스크립트, CSS, HTML 등은 LF
*.js text eol=lf
*.css text eol=lf
*.html text eol=lf

# 바이너리 파일은 Git이 처리하지 않음
*.png binary
*.jpg binary
*.zip binary